<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>

        <!--
          What shows in the gap between the splash going and the WebView's first
          paint.

          It only became visible when the splash stopped waiting for the app:
          launchAutoHide is now a 200ms timer (capacitor.config.ts), so on a slow
          cold start the window itself is on screen for a moment. The inherited
          DayNight default is white in light mode, which flashes between two
          green screens — the splash and AppLoading.

          Same green as @color/splash_background and SplashScreen.backgroundColor,
          so the three are one continuous surface and the handoff is invisible.
        -->
        <item name="android:windowBackground">@color/splash_background</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
    </style>
</resources>